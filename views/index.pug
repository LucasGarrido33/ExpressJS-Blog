extends layout

block content
  h1= title
  p Welcome to #{title}

  ul#slides.swiper-wrapper
    each post in posts
      li #{post.getTitle()},  #{post.getContent()}, #{post.getCategory().getName()}
